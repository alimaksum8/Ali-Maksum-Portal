
{
  "name": "ali-maksum-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist && cp index.html dist/ && cp *.tsx dist/ || true",
    "install": "echo 'Skipping heavy install for static ESM'"
  }
}
